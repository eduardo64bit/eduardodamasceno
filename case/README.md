# Case presentation (fonte)

Deck HTML da apresentação em detalhe publicada em **`/case`**, protegida pela mesma senha de **`/portfolio`**.

Documentação do papel dessa rota no site: [../docs/presentation-case.md](../docs/presentation-case.md)

```bash
# preview
./scripts/serve.sh

# espelhar para o Next (a partir da raiz do monorepo)
cd .. && npm run presentation:sync
```
